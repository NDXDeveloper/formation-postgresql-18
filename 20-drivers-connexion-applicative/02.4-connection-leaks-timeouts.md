🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 20.2.4. Connection Leaks et Timeouts

## Introduction

Les **connection leaks** (fuites de connexions) et les **timeouts** (délais d'attente) sont parmi les problèmes les plus courants et les plus frustrants lors du travail avec des bases de données. Dans cette section, nous allons comprendre :

- Ce qu'est un connection leak et comment il se produit
- Les différents types de timeouts
- Comment détecter ces problèmes
- Comment les prévenir et les corriger

**Pourquoi c'est important ?** Un seul connection leak peut progressivement saturer votre pool de connexions et faire tomber toute votre application.

---

## Partie 1 : Les Connection Leaks

### Qu'est-ce qu'un connection leak ?

Un **connection leak** (fuite de connexion) se produit quand une connexion est empruntée au pool mais **jamais rendue**.

**Analogie** : Imaginez une bibliothèque :
- Vous empruntez un livre (connexion)
- Vous le lisez
- Vous **oubliez de le ramener** ← Connection leak !
- La bibliothèque a un livre de moins disponible
- Après plusieurs oublis, plus de livres disponibles → Catastrophe !

### Comment ça se produit ?

#### Scénario 1 : Oubli de fermeture

**Code problématique (Python)** :
```python
from psycopg_pool import ConnectionPool

pool = ConnectionPool("postgresql://user:pass@localhost/mydb", open=False)  
pool.open(wait=True)  

def get_user(user_id):
    # Emprunter une connexion via l'API BAS-NIVEAU getconn()
    # (pool.connection() est un context manager → impossible d'oublier
    #  de rendre la connexion ; le leak n'est possible qu'avec getconn).
    conn = pool.getconn()

    # Utiliser la connexion
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
    user = cursor.fetchone()

    # OUBLI : pool.putconn(conn) jamais appelé
    return user
    # ← La connexion reste empruntée pour toujours !

# Après 10 appels à cette fonction :
# → 10 connexions perdues
# Après 20 appels avec pool_size=20 :
# → Pool complètement vide → Application bloquée !
```

**Pourquoi c'est grave ?** :
```
Appel 1  → 1 connexion perdue  
Appel 2  → 2 connexions perdues  
Appel 3  → 3 connexions perdues  
...
Appel 20 → 20 connexions perdues (pool vide)  
Appel 21 → ERREUR : Timeout waiting for connection !  
```

#### Scénario 2 : Exception non gérée

**Code problématique (Node.js)** :
```javascript
const { Pool } = require('pg');  
const pool = new Pool({ max: 10 });  

async function updateUser(userId, name) {
    const client = await pool.connect();  // Emprunter

    // Si cette requête échoue → Exception levée
    await client.query('UPDATE users SET name = $1 WHERE id = $2',
                       [name, userId]);

    client.release();  // Cette ligne n'est JAMAIS exécutée en cas d'erreur !
    // ← Connection leak !
}

// Utilisation
try {
    await updateUser(999, 'John');  // Si userId=999 n'existe pas → erreur
} catch (error) {
    console.error(error);
    // La connexion n'a pas été rendue au pool !
}
```

#### Scénario 3 : Return prématuré

**Code problématique (Java)** :
```java
public User getUser(int userId) throws SQLException {
    Connection conn = dataSource.getConnection();  // Emprunter
    Statement stmt = conn.createStatement();
    ResultSet rs = stmt.executeQuery("SELECT * FROM users WHERE id = " + userId);

    if (rs.next()) {
        User user = new User(rs.getInt("id"), rs.getString("name"));
        return user;  // Return ici !
        // ← conn.close() jamais appelé → Connection leak !
    }

    conn.close();  // Cette ligne n'est jamais atteinte
    return null;
}
```

### Conséquences d'un connection leak

#### 1. Saturation progressive du pool

```
Temps    Connexions utilisées    Connexions disponibles    État
----------------------------------------------------------------------
T+0      2                       18 (sur 20)               ✅ OK  
T+1min   5 (3 leaks)             15                        ✅ OK  
T+5min   12 (10 leaks)           8                         ⚠️ Attention  
T+10min  18 (16 leaks)           2                         ⚠️⚠️ Critique  
T+15min  20 (20 leaks)           0                         🔴 ERREUR  
→ Toutes nouvelles requêtes échouent avec timeout !
```

#### 2. Erreurs applicatives

**Messages d'erreur typiques** :

**Python (psycopg)** :
```
psycopg_pool.PoolTimeout: couldn't get a connection after 30.00 sec
```

**Node.js (pg)** :
```
TimeoutError: Timeout waiting for connection from pool
```

**Java (HikariCP)** :
```
SQLTransientConnectionException: Connection is not available, request timed out after 30000ms
```

**.NET (Npgsql)** :
```
TimeoutException: Timeout while getting a connection from pool
```

#### 3. Impact en production

```
┌─────────────────────────────────────────────────┐
│ Séquence catastrophique                         │
├─────────────────────────────────────────────────┤
│ 1. Code avec leak déployé en production         │
│ 2. Connexions s'accumulent progressivement      │
│ 3. Pool se vide                                 │
│ 4. Nouvelles requêtes échouent (timeouts)       │
│ 5. Utilisateurs voient des erreurs 500          │
│ 6. Application semble "gelée"                   │
│ 7. Redémarrage nécessaire (solution temporaire) │
│ 8. Le problème revient quelques minutes après   │
└─────────────────────────────────────────────────┘
```

### Comment détecter un connection leak ?

#### Méthode 1 : Monitoring du pool

**Python (psycopg3)** :
```python
from psycopg_pool import ConnectionPool

pool = ConnectionPool("postgresql://...", open=False)  
pool.open(wait=True)  

# Fonction de monitoring
def check_pool_health():
    stats = pool.get_stats()
    print(f"Pool total: {stats['pool_size']}")
    print(f"Disponibles: {stats['pool_available']}")
    print(f"Utilisées: {stats['pool_size'] - stats['pool_available']}")

    # Alerte si trop de connexions utilisées
    used_percent = (stats['pool_size'] - stats['pool_available']) / stats['pool_size']
    if used_percent > 0.8:
        print("⚠️ WARNING: Pool utilisation > 80%")

# Appeler régulièrement (toutes les 10 secondes)
import time  
while True:  
    check_pool_health()
    time.sleep(10)
```

**Node.js (pg)** :
```javascript
const pool = new Pool({ max: 20 });

// Monitoring du pool
setInterval(() => {
    console.log('Pool stats:');
    console.log('  Total:', pool.totalCount);
    console.log('  Idle:', pool.idleCount);
    console.log('  Waiting:', pool.waitingCount);

    const usedPercent = (pool.totalCount - pool.idleCount) / pool.totalCount;
    if (usedPercent > 0.8) {
        console.warn('⚠️ WARNING: Pool utilisation > 80%');
    }
}, 10000);
```

**Java (HikariCP)** :
```java
import com.zaxxer.hikari.HikariPoolMXBean;

// Obtenir les statistiques via JMX
HikariPoolMXBean poolMBean = hikariDataSource.getHikariPoolMXBean();

// Monitoring régulier
ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(1);  
scheduler.scheduleAtFixedRate(() -> {  
    int total = poolMBean.getTotalConnections();
    int active = poolMBean.getActiveConnections();
    int idle = poolMBean.getIdleConnections();

    System.out.println("Pool stats:");
    System.out.println("  Total: " + total);
    System.out.println("  Active: " + active);
    System.out.println("  Idle: " + idle);

    // Gérer le cas total = 0 (pool fraîchement créé) pour éviter NaN
    double usedPercent = total > 0 ? (double) active / total : 0.0;
    if (usedPercent > 0.8) {
        System.err.println("⚠️ WARNING: Pool utilisation > 80% (" + active + "/" + total + ")");
    }
}, 0, 10, TimeUnit.SECONDS);
```

#### Méthode 2 : Logs PostgreSQL

**Activer les logs de connexion/déconnexion** :

**postgresql.conf** :
```conf
log_connections = on  
log_disconnections = on  
log_line_prefix = '%t [%p] %u@%d '  
```

**Analyser les logs** :
```bash
# Compter les connexions vs déconnexions
grep "connection authorized" /var/log/postgresql/postgresql-*.log | wc -l  
grep "disconnection" /var/log/postgresql/postgresql-*.log | wc -l  

# Si connexions >> déconnexions → Leak probable
```

**Exemple de sortie** :
```
Connexions : 1523  
Déconnexions : 1234  
Différence : 289 connexions non fermées → LEAK !  
```

#### Méthode 3 : Requête PostgreSQL

**Voir les connexions par âge** :
```sql
SELECT
    pid,
    usename,
    application_name,
    client_addr,
    state,
    now() - backend_start as age,
    query
FROM pg_stat_activity  
WHERE datname = current_database()  
ORDER BY backend_start  
LIMIT 20;  
```

**Indicateurs de leak** :
```
 pid  | usename | app_name | state |    age     | query
------+---------+----------+-------+------------+-------
 1234 | myapp   | MyAPI    | idle  | 02:15:32   | SELECT ...
 1235 | myapp   | MyAPI    | idle  | 02:10:45   | UPDATE ...
 1236 | myapp   | MyAPI    | idle  | 02:05:12   | INSERT ...
```

Si vous voyez des connexions `idle` depuis **des heures** → Leak probable !

### Comment corriger un connection leak ?

#### Solution 1 : Toujours utiliser try/finally

**Python - Version CORRECTE (try/finally bas-niveau)** :
```python
def get_user(user_id):
    conn = pool.getconn()   # API bas niveau (vs pool.connection() qui est un context manager)
    try:
        with conn.cursor() as cursor:
            cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
            return cursor.fetchone()
    finally:
        pool.putconn(conn)  # TOUJOURS rendre la connexion au pool
```

> **Attention** : `pool.connection()` retourne un *context manager* — `conn = pool.connection()` ne donne PAS une connexion, mais l'objet manager. Pour un usage manuel, c'est `pool.getconn()` / `pool.putconn(conn)`.

**Encore mieux : Context manager (with)** :
```python
def get_user(user_id):
    with pool.connection() as conn:  # Fermeture automatique
        with conn.cursor() as cursor:
            cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
            return cursor.fetchone()
    # Connexion automatiquement rendue au pool ici
```

#### Solution 2 : Try/catch/finally en Node.js

**Node.js - Version CORRECTE** :
```javascript
async function updateUser(userId, name) {
    const client = await pool.connect();

    try {
        await client.query(
            'UPDATE users SET name = $1 WHERE id = $2',
            [name, userId]
        );
    } catch (error) {
        console.error('Error updating user:', error);
        throw error;
    } finally {
        client.release();  // TOUJOURS exécuté
    }
}
```

**Encore mieux : Utiliser pool.query()** :
```javascript
// pool.query() gère automatiquement connect() et release()
async function updateUser(userId, name) {
    await pool.query(
        'UPDATE users SET name = $1 WHERE id = $2',
        [name, userId]
    );
    // Pas de leak possible !
}
```

#### Solution 3 : Try-with-resources en Java

**Java - Version CORRECTE** :
```java
public User getUser(int userId) throws SQLException {
    // try-with-resources : fermeture automatique + PreparedStatement
    // (concaténer userId dans le SQL ouvrirait une injection SQL — toujours utiliser des paramètres)
    String sql = "SELECT * FROM users WHERE id = ?";
    try (Connection conn = dataSource.getConnection();
         PreparedStatement pstmt = conn.prepareStatement(sql)) {

        pstmt.setInt(1, userId);
        try (ResultSet rs = pstmt.executeQuery()) {
            if (rs.next()) {
                return new User(rs.getInt("id"), rs.getString("name"));
            }
            return null;
        }
    }
    // conn et pstmt sont automatiquement fermés ici, même avec return ou exception
}
```

#### Solution 4 : Using statement en .NET

**.NET - Version CORRECTE** :
```csharp
public User GetUser(int userId)
{
    using (var conn = new NpgsqlConnection(connectionString))
    using (var cmd = new NpgsqlCommand("SELECT * FROM users WHERE id = @id", conn))
    {
        conn.Open();
        cmd.Parameters.AddWithValue("id", userId);

        using (var reader = cmd.ExecuteReader())
        {
            if (reader.Read())
            {
                return new User
                {
                    Id = reader.GetInt32(0),
                    Name = reader.GetString(1)
                };
            }
        }
    }
    // Connexion automatiquement fermée
    return null;
}
```

#### Solution 5 : defer en Go

**Go - Version CORRECTE** :
```go
func getUser(ctx context.Context, userId int) (*User, error) {
    conn, err := pool.Acquire(ctx)
    if err != nil {
        return nil, err
    }
    defer conn.Release()  // Exécuté à la fin de la fonction

    var user User
    err = conn.QueryRow(ctx,
        "SELECT id, name FROM users WHERE id = $1",
        userId,
    ).Scan(&user.ID, &user.Name)

    if err != nil {
        return nil, err
    }
    return &user, nil
}
```

### Prévention des connection leaks

#### Pattern 1 : Repository Pattern

**Principe** : Centraliser l'accès aux données dans une couche dédiée.

**Python - Exemple** :
```python
class UserRepository:
    def __init__(self, pool):
        self.pool = pool

    def get_user(self, user_id):
        """Méthode qui gère automatiquement les connexions"""
        with self.pool.connection() as conn:
            with conn.cursor() as cursor:
                cursor.execute(
                    "SELECT id, name, email FROM users WHERE id = %s",
                    (user_id,)
                )
                return cursor.fetchone()

    def update_user(self, user_id, name, email):
        # `with pool.connection() as conn` gère déjà COMMIT/ROLLBACK
        # automatiquement à la sortie : pas besoin de `conn.commit()` manuel.
        with self.pool.connection() as conn:
            with conn.cursor() as cursor:
                cursor.execute(
                    "UPDATE users SET name = %s, email = %s WHERE id = %s",
                    (name, email, user_id)
                )

# Utilisation (pas de gestion de connexion manuelle)
repo = UserRepository(pool)  
user = repo.get_user(42)  
repo.update_user(42, "Alice", "alice@example.com")  
```

#### Pattern 2 : Decorators/Wrappers

**Python - Décorateur** :
```python
from functools import wraps

def with_connection(func):
    """Décorateur qui gère automatiquement les connexions"""
    @wraps(func)
    def wrapper(*args, **kwargs):
        with pool.connection() as conn:
            return func(conn, *args, **kwargs)
    return wrapper

# Utilisation
@with_connection
def get_user(conn, user_id):
    with conn.cursor() as cursor:
        cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
        return cursor.fetchone()

# Pas besoin de gérer la connexion manuellement
user = get_user(42)
```

#### Pattern 3 : ORM (Object-Relational Mapping)

Les ORMs gèrent automatiquement les connexions :

**Python - SQLAlchemy** :
```python
from sqlalchemy import create_engine  
from sqlalchemy.orm import sessionmaker, Session  

engine = create_engine("postgresql://user:pass@localhost/mydb")  
SessionLocal = sessionmaker(bind=engine)  

def get_user(user_id: int):
    session = SessionLocal()
    try:
        user = session.query(User).filter(User.id == user_id).first()
        return user
    finally:
        session.close()  # Gère la connexion

# Ou avec context manager
def get_user(user_id: int):
    with SessionLocal() as session:
        return session.query(User).filter(User.id == user_id).first()
```

**Node.js - Sequelize** :
```javascript
const { Sequelize, Model, DataTypes } = require('sequelize');

const sequelize = new Sequelize('postgresql://user:pass@localhost/mydb');

class User extends Model {}  
User.init({  
    name: DataTypes.STRING,
    email: DataTypes.STRING
}, { sequelize });

// Sequelize gère les connexions automatiquement
async function getUser(userId) {
    return await User.findByPk(userId);
    // Pas de gestion de connexion manuelle !
}
```

---

## Partie 2 : Les Timeouts

### Qu'est-ce qu'un timeout ?

Un **timeout** est un délai maximal d'attente après lequel une opération échoue.

**Analogie** : Vous attendez le bus. Après 30 minutes (timeout), vous décidez que le bus ne viendra pas et vous prenez un taxi.

### Types de timeouts

#### 1. Connection Timeout

**Définition** : Temps maximum pour **établir** une connexion au serveur PostgreSQL.

**Configuration** :

**Python (psycopg3)** :
```python
import psycopg

# Timeout de 5 secondes pour se connecter
conn = psycopg.connect(
    "postgresql://user:pass@localhost/mydb",
    connect_timeout=5
)
```

**Node.js (pg)** :
```javascript
const pool = new Pool({
    connectionTimeoutMillis: 5000  // 5 secondes
});
```

**Java (HikariCP)** :
```java
config.setConnectionTimeout(5000);  // 5 secondes
```

**Quand il se déclenche** :
- Serveur PostgreSQL injoignable
- Problèmes réseau
- Pare-feu bloquant

**Erreur typique** :
```
psycopg.OperationalError: connection to server at "localhost" (::1), port 5432 failed:  
timeout expired  
```

#### 2. Pool Timeout (Pool Exhaustion)

**Définition** : Temps maximum d'attente pour **obtenir une connexion du pool**.

**Configuration** :

**Python (psycopg3)** :
```python
from psycopg_pool import ConnectionPool

pool = ConnectionPool(
    conninfo="postgresql://...",
    timeout=30,        # Attendre max 30 secondes
    open=False,        # Recommandé depuis psycopg_pool 3.2
)
pool.open(wait=True)   # Ouverture explicite
```

**Node.js (pg)** :
```javascript
const pool = new Pool({
    connectionTimeoutMillis: 30000  // 30 secondes
});
```

**Java (HikariCP)** :
```java
config.setConnectionTimeout(30000);  // 30 secondes
```

**Quand il se déclenche** :
- Pool saturé (toutes les connexions utilisées)
- Connection leaks en cours
- Requêtes lentes monopolisant les connexions

**Erreur typique** :
```
PoolTimeout: couldn't get a connection after 30.00 sec
```

**Diagramme** :
```
Pool (10 connexions)
├─ Connexion 1 : UTILISÉE (requête en cours)
├─ Connexion 2 : UTILISÉE
├─ Connexion 3 : UTILISÉE
├─ ...
├─ Connexion 10 : UTILISÉE
└─ [AUCUNE DISPONIBLE]

Nouvelle requête arrive → Attend une connexion libre
⏱️ 30 secondes passent → Toujours rien
→ PoolTimeout !
```

#### 3. Statement Timeout

**Définition** : Temps maximum d'**exécution** d'une requête SQL.

**Configuration** :

**PostgreSQL (postgresql.conf)** :
```conf
statement_timeout = 30000  # 30 secondes (en millisecondes)
```

**Ou par session** :
```sql
SET statement_timeout = 30000;  -- 30 secondes

-- Ou pour une base spécifique
ALTER DATABASE mydb SET statement_timeout = 30000;
```

**Python (psycopg3)** :
```python
with pool.connection() as conn:
    with conn.transaction():                    # BEGIN ... COMMIT implicite
        # SET LOCAL = limité à la transaction, RESET automatique au COMMIT.
        # ⚠️ Sans LOCAL, le SET resterait sur la connexion une fois rendue au pool
        # et "fuiterait" vers les prochains emprunts !
        conn.execute("SET LOCAL statement_timeout = 30000")
        conn.execute("SELECT * FROM big_table")
```

> **Alternative globale (recommandée)** : configurer `statement_timeout` côté serveur via  
> `ALTER DATABASE mydb SET statement_timeout = 30000;` ou par `options=-c statement_timeout=30000`  
> dans la connection string — pas de risque de fuite entre emprunts du pool.

**Quand il se déclenche** :
- Requête trop lente (full table scan)
- Jointures complexes sur grandes tables
- Deadlocks
- Requêtes mal optimisées

**Erreur typique** :
```sql
ERROR: canceling statement due to statement timeout
```

#### 4. Idle In Transaction Timeout

**Définition** : Temps maximum qu'une transaction peut rester **ouverte sans activité**.

**Configuration** :

**PostgreSQL (postgresql.conf)** :
```conf
idle_in_transaction_session_timeout = 300000  # 5 minutes
```

**Exemple de problème** :
```python
# Code PROBLÉMATIQUE
# (pool.getconn() : API bas-niveau qui retourne une vraie connexion,
#  contrairement à pool.connection() qui retourne un context manager)
conn = pool.getconn()  
conn.execute("BEGIN")  
conn.execute("UPDATE users SET name = 'Alice' WHERE id = 1")  

# Oubli de COMMIT ou ROLLBACK + oubli de pool.putconn(conn)
# La transaction reste ouverte ET la connexion n'est pas rendue au pool...
time.sleep(600)  # 10 minutes !

# → idle_in_transaction_session_timeout se déclenche côté PostgreSQL
# → Transaction annulée (mais connexion reste empruntée côté pool)
```

**Pourquoi c'est important** :
- Empêche les transactions zombies
- Libère les verrous (locks)
- Évite le blocage d'autres transactions

#### 5. Lock Timeout

**Définition** : Temps maximum d'attente pour acquérir un **verrou** (lock).

**Configuration** :
```sql
SET lock_timeout = 5000;  -- 5 secondes
```

**Exemple** :
```sql
-- Transaction 1
BEGIN;  
UPDATE users SET name = 'Bob' WHERE id = 1;  
-- Ne COMMIT pas → Garde le verrou

-- Transaction 2 (connexion différente)
BEGIN;  
SET lock_timeout = 5000;  
UPDATE users SET name = 'Alice' WHERE id = 1;  -- Attend le verrou  
-- Après 5 secondes → lock_timeout !
ERROR: canceling statement due to lock timeout
```

### Configuration recommandée des timeouts

#### Configuration PostgreSQL globale

**postgresql.conf** :
```conf
# Connexion
tcp_keepalives_idle = 60              # Ping toutes les 60s  
tcp_keepalives_interval = 10          # Retry toutes les 10s  
tcp_keepalives_count = 6              # 6 retries avant abandon  

# Requêtes
statement_timeout = 60000             # 60 secondes max par requête  
idle_in_transaction_session_timeout = 300000  # 5 minutes  

# Verrous
lock_timeout = 10000                  # 10 secondes  
deadlock_timeout = 1000               # 1 seconde  
```

#### Configuration par application

**API REST (temps de réponse rapide attendu)** :
```python
# Configurer les timeouts dans la connection string via `options=-c key=value` :
# ces valeurs sont appliquées à CHAQUE connexion du pool, sans risque de fuite
# vers les autres requêtes (contrairement à un SET sans LOCAL exécuté à l'usage).
pool = ConnectionPool(
    conninfo=(
        "postgresql://user:pass@localhost/mydb"
        "?options=-c%20statement_timeout%3D10000"   # 10s max par requête
        "%20-c%20lock_timeout%3D2000"               # 2s max pour lock
    ),
    timeout=5,           # 5s pour obtenir une connexion du pool
    max_size=20,
    open=False,          # Recommandé depuis psycopg_pool 3.2
)
pool.open(wait=True)

# Alternative : SET LOCAL dans une transaction pour des timeouts par requête
with pool.connection() as conn:
    with conn.transaction():
        conn.execute("SET LOCAL statement_timeout = 10000")  # OK : LOCAL = transaction
        conn.execute("SELECT ...")
```

**Application batch (requêtes longues autorisées)** :
```python
# Pool dédié batch avec timeouts longs : pas de risque de "fuite" puisque
# ce pool n'est utilisé QUE par les jobs batch.
pool_batch = ConnectionPool(
    conninfo=(
        "postgresql://user:pass@localhost/mydb"
        "?options=-c%20statement_timeout%3D600000"  # 10 min max
        "%20-c%20lock_timeout%3D30000"              # 30s max pour lock
    ),
    timeout=30,
    max_size=5,
    open=False,
)
pool_batch.open(wait=True)
```

> **Pourquoi `options=-c …` plutôt que `conn.execute("SET …")` ?** Quand on configure  
> via la connection string, PostgreSQL applique les paramètres au démarrage de  
> chaque connexion du pool. Un `SET` (sans LOCAL) sur une connexion empruntée  
> **persiste** quand la connexion retourne au pool — et "fuite" vers les  
> prochains emprunts. C'est un bug fréquent et silencieux.

### Gestion des timeouts dans le code

#### Pattern 1 : Retry avec backoff

**Python - Retry simple** :
```python
import time  
from psycopg_pool import PoolTimeout  

def execute_with_retry(func, max_retries=3):
    """Réessaie en cas de timeout"""
    for attempt in range(max_retries):
        try:
            return func()
        except PoolTimeout:
            if attempt < max_retries - 1:
                wait_time = 2 ** attempt  # Backoff exponentiel: 1s, 2s, 4s
                print(f"Timeout, retry in {wait_time}s...")
                time.sleep(wait_time)
            else:
                raise  # Dernier essai raté, propagation de l'erreur

# Utilisation
def get_user():
    with pool.connection() as conn:
        with conn.cursor() as cursor:
            cursor.execute("SELECT * FROM users WHERE id = 1")
            return cursor.fetchone()

user = execute_with_retry(get_user)
```

**Node.js - Retry avec bibliothèque** :
```javascript
const retry = require('async-retry');

// Codes d'erreur considérés comme transitoires (à retenter)
// - 57014 (PostgreSQL SQLSTATE) : query_canceled (ex. statement_timeout)
// - 57P03 : cannot_connect_now (serveur en cours de démarrage)
// - 08000, 08003, 08006 : connection_exception
// - ETIMEDOUT, ECONNRESET (Node.js, errno réseau)
const TRANSIENT_CODES = new Set([
    '57014', '57P03', '08000', '08003', '08006',
    'ETIMEDOUT', 'ECONNRESET'
]);

async function getUserWithRetry(userId) {
    return await retry(
        async (bail) => {
            try {
                const result = await pool.query(
                    'SELECT * FROM users WHERE id = $1',
                    [userId]
                );
                return result.rows[0];
            } catch (error) {
                // ✅ Comparer le code SQLSTATE / errno plutôt que parser le message
                //    (les messages peuvent changer entre versions / langues).
                if (TRANSIENT_CODES.has(error.code)) {
                    throw error;  // Retry
                } else {
                    bail(error);  // Erreur non-recoverable, arrêt
                }
            }
        },
        {
            retries: 3,
            factor: 2,      // Backoff exponentiel : 1s, 2s, 4s
            minTimeout: 1000
        }
    );
}
```

#### Pattern 2 : Circuit Breaker

**Principe** : Arrêter d'essayer si trop d'échecs consécutifs.

```
┌─────────────────────────────────────────────────┐
│ États du Circuit Breaker                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  [CLOSED] ──(5 échecs)──► [OPEN]                │
│     ↑                         │                 │
│     │                         │ (1 minute)      │
│     │                         ▼                 │
│     └───(succès)────── [HALF-OPEN]              │
│                                                 │
│  CLOSED    : Trafic normal                      │
│  OPEN      : Rejette immédiatement              │
│  HALF-OPEN : Teste avec 1 requête               │
└─────────────────────────────────────────────────┘
```

**Python - Implémentation simple** :
```python
import time  
import threading  
from enum import Enum  

class CircuitState(Enum):
    CLOSED = "closed"
    OPEN = "open"
    HALF_OPEN = "half_open"

class CircuitBreaker:
    def __init__(self, failure_threshold=5, timeout=60):
        self.failure_threshold = failure_threshold
        self.timeout = timeout
        self.failures = 0
        self.last_failure_time = None
        self.state = CircuitState.CLOSED
        self._lock = threading.Lock()  # Thread-safety pour multi-thread

    def call(self, func):
        # monotonic() : non affecté par les changements d'horloge système
        with self._lock:
            if self.state == CircuitState.OPEN:
                if time.monotonic() - self.last_failure_time >= self.timeout:
                    self.state = CircuitState.HALF_OPEN
                else:
                    raise Exception("Circuit breaker is OPEN")

        try:
            result = func()
        except Exception:
            with self._lock:
                self.failures += 1
                self.last_failure_time = time.monotonic()
                if self.failures >= self.failure_threshold:
                    self.state = CircuitState.OPEN
            raise  # `raise` sans variable → préserve la stack trace originale

        with self._lock:
            self.failures = 0
            self.state = CircuitState.CLOSED
        return result

# Utilisation
breaker = CircuitBreaker(failure_threshold=5, timeout=60)

def query_database():
    with pool.connection() as conn:
        # Requête...
        pass

try:
    result = breaker.call(query_database)
except Exception as e:
    print(f"Circuit breaker prevented call or call failed: {e}")
```

> **Pour la production** : préférer une bibliothèque éprouvée — `pybreaker` (Python), `resilience4j` (Java), `opossum` (Node.js), `Polly` (.NET). Elles gèrent les détails subtils (fenêtre glissante, demi-fermeture probabiliste, métriques).

#### Pattern 3 : Timeout granulaire

> ⚠️ **Anti-pattern fréquent** : `Promise.race([pool.query(...), setTimeout(...)])` rejette la promesse côté client mais **la requête continue d'exécuter côté PostgreSQL** (la connexion reste occupée jusqu'à la fin). Pour vraiment annuler la requête, il faut passer par `statement_timeout` côté serveur — c'est PostgreSQL qui interrompt l'exécution.

**Node.js (pg) - Timeout via `statement_timeout` côté serveur :**
```javascript
// ⚠️ Validation : timeoutMs doit être un entier positif, sinon injection SQL possible
//    car on l'interpole dans le SET (les valeurs de GUC ne sont pas paramétrables via $1).
function queryWithTimeout(sql, params, timeoutMs = 10000) {
    if (!Number.isInteger(timeoutMs) || timeoutMs < 0) {
        throw new Error('timeoutMs doit être un entier positif (millisecondes)');
    }
    return _queryWithTimeout(sql, params, timeoutMs);
}

async function _queryWithTimeout(sql, params, timeoutMs) {
    const client = await pool.connect();
    try {
        // SET LOCAL nécessite d'être DANS une transaction ; hors transaction
        // PostgreSQL ignore silencieusement le SET LOCAL (warning).
        // → on enveloppe dans BEGIN/COMMIT pour que LOCAL prenne effet.
        await client.query('BEGIN');
        await client.query(`SET LOCAL statement_timeout = ${timeoutMs}`);
        const result = await client.query(sql, params);
        await client.query('COMMIT');
        return result;
    } catch (err) {
        // ROLLBACK est inoffensif si la transaction est déjà abortée (57014, etc.)
        await client.query('ROLLBACK').catch(() => {});
        throw err;
    } finally {
        client.release();   // statement_timeout est reset car LOCAL = transaction
    }
}

try {
    const result = await queryWithTimeout(
        'SELECT * FROM large_table',
        [],
        10000  // 10 secondes max — PostgreSQL interrompra réellement la requête
    );
} catch (error) {
    // Code SQLSTATE 57014 = query_canceled (par statement_timeout)
    if (error.code === '57014') {
        console.error('Requête trop lente, annulée par PostgreSQL');
    } else {
        throw error;
    }
}
```

**Alternative : configurer `query_timeout` globalement sur le pool :**
```javascript
const pool = new Pool({
    connectionString: process.env.DATABASE_URL,
    query_timeout: 10000,           // côté client (annule la promesse + close socket)
    statement_timeout: 10000,       // côté serveur (transmis comme paramètre de connexion)
});
```

---

## Diagnostic et troubleshooting

### Détecter un connection leak en production

#### Checklist de diagnostic

**1. Vérifier l'utilisation du pool** :
```python
# Python
stats = pool.get_stats()  
print(f"Disponibles: {stats['pool_available']}")  
print(f"Utilisées: {stats['pool_size'] - stats['pool_available']}")  
```

**2. Vérifier les connexions PostgreSQL** :
```sql
-- Connexions par état
SELECT state, count(*)  
FROM pg_stat_activity  
WHERE datname = current_database()  
GROUP BY state;  

-- Résultat suspect :
--  state  | count
-- --------+-------
--  idle   | 87     ← BEAUCOUP trop !
--  active | 3
```

**3. Vérifier les connexions anciennes** :
```sql
-- Connexions ouvertes depuis longtemps
SELECT
    pid,
    usename,
    application_name,
    state,
    now() - backend_start as duration
FROM pg_stat_activity  
WHERE state = 'idle'  
  AND now() - backend_start > interval '10 minutes'
ORDER BY duration DESC;
```

**4. Logs applicatifs** :
```bash
# Chercher les timeouts
grep -i "timeout" /var/log/myapp/app.log

# Chercher les erreurs de pool
grep -i "pool" /var/log/myapp/app.log
```

### Solutions d'urgence en production

#### Solution 1 : Redémarrage applicatif

**Le plus rapide (mais temporaire)** :
```bash
# Redémarrer l'application
systemctl restart myapp

# Ou en Kubernetes
kubectl rollout restart deployment/myapp
```

**Effet** : Nettoie les connexions leaked, mais le problème reviendra.

#### Solution 2 : Tuer les connexions idle

**Côté PostgreSQL** :
```sql
-- Tuer toutes les connexions idle depuis plus de 1 heure
-- pid <> pg_backend_pid() : exclure la session psql qui exécute cette requête
SELECT pg_terminate_backend(pid)  
FROM pg_stat_activity  
WHERE datname = current_database()  
  AND state = 'idle'
  AND pid <> pg_backend_pid()
  AND now() - state_change > interval '1 hour';
```

**⚠️ Attention** : Ceci peut casser des applications qui attendent ces connexions.

#### Solution 3 : Augmenter temporairement le pool

**PgBouncer** :
```sql
-- Se connecter à la console admin
psql -h localhost -p 6432 -U admin pgbouncer

-- Augmenter temporairement le pool
SET default_pool_size = 50;  -- Au lieu de 20

-- Recharger
RELOAD;
```

**Application** :
```python
# Modifier temporairement et redéployer
pool = ConnectionPool(
    conninfo="...",
    max_size=40,  # Au lieu de 20
    open=False,
)
pool.open(wait=True)
```

### Outils de debugging

#### 1. py-spy (Python)

```bash
# Installer
pip install py-spy

# Profiler l'application en cours
sudo py-spy top --pid 12345

# Voir où le code est bloqué
sudo py-spy dump --pid 12345
```

#### 2. Node.js - clinic.js

```bash
# Installer
npm install -g clinic

# Profiler
clinic doctor -- node app.js

# Analyser
clinic doctor --analyze
```

#### 3. Java - JConsole / VisualVM

```bash
# Lancer JConsole
jconsole

# Se connecter au processus Java
# Aller dans MBeans → HikariPool
# Voir : ActiveConnections, IdleConnections, ThreadsAwaitingConnection
```

#### 4. Logs structurés

**Python - Loguru** :
```python
from loguru import logger

# Configuration
logger.add(
    "app.log",
    format="{time} {level} {message}",
    rotation="100 MB"
)

# Utilisation
def get_user(user_id):
    logger.info(f"Connection acquiring for user {user_id}")

    with pool.connection() as conn:
        logger.info("Connection acquired")
        # Requête... (si elle crash, le log "released" ci-dessous NE PARAÎT PAS,
        # ce qui permet de détecter le problème en aval)
        result = conn.execute("SELECT ...").fetchone()
        # Le `with` rend la connexion au pool en sortie de bloc

    # ⚠️ Log "released" APRÈS le `with` pour refléter la réalité
    #    (sortie du with = retour effectif au pool).
    logger.info("Connection released")
    return result
```

Analyser ensuite :
```bash
# Compter les acquisitions et libérations
acquired=$(grep -c "Connection acquired" app.log)  
released=$(grep -c "Connection released" app.log)  

if [ "$acquired" -ne "$released" ]; then
    echo "⚠️  LEAK probable : $acquired acquisitions vs $released libérations"
fi
```

---

## Bonnes pratiques - Checklist complète

### ✅ Prévention des connection leaks

```
☐ Toujours utiliser les gestionnaires de contexte
   Python: with statement
   Java: try-with-resources
   .NET: using statement
   Go: defer
   Node.js: try/finally

☐ Préférer les méthodes de haut niveau
   pool.query() au lieu de connect() + release()

☐ Implémenter le Repository Pattern
   Centraliser l'accès aux données

☐ Utiliser un ORM si approprié
   SQLAlchemy, Sequelize, Hibernate, Entity Framework

☐ Ajouter des tests automatisés
   Vérifier qu'il n'y a pas de leak après chaque test
```

### ✅ Configuration des timeouts

```
☐ Connection timeout : 5-10 secondes
   Échec rapide si serveur injoignable

☐ Pool timeout : 10-30 secondes
   Assez long pour pics de charge

☐ Statement timeout : 30-60 secondes (API)
   Empêche les requêtes infinies

☐ Statement timeout : 5-10 minutes (batch)
   Pour traitement de masse

☐ idle_in_transaction_timeout : 5 minutes
   Nettoie les transactions zombies

☐ lock_timeout : 5-10 secondes
   Évite les blocages prolongés
```

### ✅ Monitoring

```
☐ Surveiller l'utilisation du pool
   Alerte si > 80%

☐ Tracer les connexions PostgreSQL
   Idle, active, idle in transaction

☐ Logger les timeouts applicatifs
   Identifier les patterns

☐ Dashboard temps réel
   Grafana + Prometheus

☐ Alerting proactif
   Avant saturation complète
```

### ✅ Testing

```
☐ Tests de charge
   Simuler charge réaliste

☐ Tests de saturation
   Que se passe-t-il si pool vide ?

☐ Tests de leak
   Vérifier libération après tests

☐ Tests de timeout
   Comportement en cas de lenteur

☐ Tests de résilience
   Circuit breaker, retry, fallback
```

---

## Scénarios réels et solutions

### Scénario 1 : API qui se "gèle" après quelques heures

**Symptômes** :
- Application démarre bien
- Après 2-3 heures : timeouts, erreurs 500
- Redémarrage → ça repart pendant 2-3 heures

**Diagnostic** :
```sql
-- Beaucoup de connexions idle
SELECT count(*) FROM pg_stat_activity WHERE state = 'idle';
-- Résultat : 95 (sur pool_size=20 → Multiplication anormale !)
```

**Cause** : Connection leak dans le code

**Solution** :
```python
# AVANT (incorrect) — getconn() emprunte sans rendre la connexion
def process_data():
    conn = pool.getconn()     # API bas-niveau : retourne une connexion brute
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM data")
    return cursor.fetchall()
    # Oubli de pool.putconn(conn) → LEAK !

# APRÈS (correct) — context manager, libération automatique
def process_data():
    with pool.connection() as conn:
        with conn.cursor() as cursor:
            cursor.execute("SELECT * FROM data")
            return cursor.fetchall()
```

### Scénario 2 : Timeouts aléatoires sous charge

**Symptômes** :
- Application normale en charge faible
- Sous charge : timeouts sporadiques
- pool_size = 10, max_connections = 100

**Diagnostic** :
```javascript
// Node.js monitoring
console.log(pool.waitingCount);  // 45 requêtes en attente !
```

**Cause** : Pool trop petit

**Solution** :
```javascript
// Augmenter le pool
const pool = new Pool({
    max: 20,  // Au lieu de 10
    min: 5
});

// Ou introduire PgBouncer
```

### Scénario 3 : Deadlocks fréquents

**Symptômes** :
```
ERROR: deadlock detected  
DETAIL: Process 1234 waits for ShareLock on transaction 5678;  
        blocked by process 5678.
```

**Diagnostic** :
```sql
-- Voir les verrous
SELECT * FROM pg_locks WHERE NOT granted;
```

**Solution** :
```sql
-- Configurer lock_timeout
SET lock_timeout = 5000;  -- 5 secondes

-- Dans l'application, ordonner les accès
-- TOUJOURS dans le même ordre : users puis orders
-- Jamais : orders puis users dans une transaction
--          et users puis orders dans une autre
```

### Scénario 4 : Transactions zombies

**Symptômes** :
```sql
SELECT count(*) FROM pg_stat_activity  
WHERE state = 'idle in transaction';  
-- Résultat : 23 !
```

**Cause** : Transactions ouvertes jamais commitées

**Solution** :
```conf
# postgresql.conf
idle_in_transaction_session_timeout = 300000  # 5 minutes
```

```python
# Dans le code (psycopg3) : utiliser conn.transaction(), pas BEGIN/COMMIT manuel.
# Le context manager gère COMMIT (sortie normale) / ROLLBACK (exception) automatiquement.
with pool.connection() as conn:
    with conn.transaction():
        # Opérations... — la transaction est implicite ici
        conn.execute("UPDATE comptes SET solde = solde - 100 WHERE id = 1")
        conn.execute("UPDATE comptes SET solde = solde + 100 WHERE id = 2")
    # Sortie du `with conn.transaction()` :
    #   - sans exception → COMMIT
    #   - avec exception → ROLLBACK + propagation
```

---

## Résumé : Points clés à retenir

### 🔑 Connection Leaks

**Causes principales** :
1. Oubli de fermeture (conn.close())  
2. Exception non gérée  
3. Return prématuré

**Solutions** :
1. ✅ Toujours utiliser with/try-with-resources/using/defer  
2. ✅ Préférer les méthodes de haut niveau (pool.query())  
3. ✅ Implémenter Repository Pattern  
4. ✅ Monitorer continuellement le pool

### ⏱️ Timeouts

**Types principaux** :
1. **Connection timeout** : Établir connexion (5-10s)  
2. **Pool timeout** : Obtenir connexion du pool (10-30s)  
3. **Statement timeout** : Exécuter requête (30s-10min)  
4. **Idle in transaction** : Transaction inactive (5min)  
5. **Lock timeout** : Acquérir verrou (5-10s)

**Configuration recommandée** :
```conf
# postgresql.conf
statement_timeout = 60000  # 1 minute  
idle_in_transaction_session_timeout = 300000  # 5 minutes  
lock_timeout = 10000  # 10 secondes  
```

### 🎯 Patterns essentiels

1. **Always Close** : Toujours fermer les connexions  
2. **Retry with Backoff** : Réessayer intelligemment  
3. **Circuit Breaker** : Arrêter si trop d'échecs  
4. **Monitoring** : Surveiller en continu  
5. **Graceful Degradation** : Dégrader plutôt que crasher

---

## Pour aller plus loin

### Documentation

- [PostgreSQL Timeouts](https://www.postgresql.org/docs/current/runtime-config-client.html)  
- [HikariCP Connection Leak Detection](https://github.com/brettwooldridge/HikariCP/wiki/Leak-Detection)  
- [psycopg3 Connection Pool](https://www.psycopg.org/psycopg3/docs/advanced/pool.html)

### Outils recommandés

- **Sentry** : Tracking d'erreurs et timeouts  
- **DataDog / New Relic** : APM et monitoring  
- **Grafana** : Dashboards de monitoring  
- **PgHero** : Health dashboard PostgreSQL

---


⏭️ [Patterns anti-corruption](/20-drivers-connexion-applicative/03-patterns-anti-corruption.md)
