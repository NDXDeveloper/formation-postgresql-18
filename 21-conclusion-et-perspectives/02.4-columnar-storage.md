🔝 Retour au [Sommaire](/SOMMAIRE.md)

# 21.2.4 Columnar Storage : Hydra et pg_analytics

## Introduction

Les bases de données traditionnelles, dont PostgreSQL, stockent les données **ligne par ligne** (row-oriented). Cette approche est optimale pour les opérations transactionnelles (OLTP) mais devient inefficace pour l'analytique sur de grands volumes de données.

Le **stockage colonnaire** (columnar storage) offre une alternative révolutionnaire pour les workloads analytiques (OLAP). Ce chapitre explore comment PostgreSQL s'enrichit de capacités colonnaires grâce à des extensions comme Hydra et pg_analytics, transformant PostgreSQL en une plateforme unifiée pour les transactions ET l'analytique.

---

## Partie 1 : Comprendre le Stockage Colonnaire

### 1.1 Row Store vs Column Store

La différence fondamentale réside dans l'organisation physique des données sur le disque.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Row Store (PostgreSQL Standard)                      │
│                                                                         │
│   Table "ventes" :                                                      │
│   ┌────────┬────────────┬──────────┬─────────┬──────────┐               │
│   │   id   │    date    │ produit  │ quantité│  montant │               │
│   ├────────┼────────────┼──────────┼─────────┼──────────┤               │
│   │   1    │ 2025-01-15 │  Widget  │    10   │   99.90  │               │
│   │   2    │ 2025-01-15 │  Gadget  │     5   │  149.95  │               │
│   │   3    │ 2025-01-16 │  Widget  │     8   │   79.92  │               │
│   └────────┴────────────┴──────────┴─────────┴──────────┘               │
│                                                                         │
│   Stockage sur disque (par ligne) :                                     │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │ [1|2025-01-15|Widget|10|99.90][2|2025-01-15|Gadget|5|149.95]... │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ✓ Optimal pour : SELECT * FROM ventes WHERE id = 1                    │
│   ✗ Inefficace pour : SELECT SUM(montant) FROM ventes                   │
│     (doit lire TOUTES les colonnes pour chaque ligne)                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    Column Store (Stockage Colonnaire)                   │
│                                                                         │
│   Même table "ventes", stockée différemment :                           │
│                                                                         │
│   Stockage sur disque (par colonne) :                                   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │ id:       [1, 2, 3, 4, 5, ...]                                  │   │
│   │ date:     [2025-01-15, 2025-01-15, 2025-01-16, ...]             │   │
│   │ produit:  [Widget, Gadget, Widget, ...]                         │   │
│   │ quantité: [10, 5, 8, ...]                                       │   │
│   │ montant:  [99.90, 149.95, 79.92, ...]                           │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   ✓ Optimal pour : SELECT SUM(montant) FROM ventes                      │
│     (lit UNIQUEMENT la colonne montant)                                 │
│   ✗ Moins efficace pour : SELECT * FROM ventes WHERE id = 1             │
│     (doit reconstruire la ligne depuis plusieurs colonnes)              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 Avantages du Stockage Colonnaire

| Avantage | Description | Impact |
|----------|-------------|--------|
| **Lecture sélective** | Lit uniquement les colonnes nécessaires | I/O réduit de 10-100× |
| **Compression** | Valeurs similaires groupées = meilleure compression | Stockage réduit de 5-10× |
| **Vectorisation** | Traitement par lots de valeurs | CPU plus efficace |
| **Cache efficace** | Données utiles restent en cache | Moins d'accès disque |
| **Agrégations rapides** | SUM, AVG, COUNT optimisés | Requêtes 10-1000× plus rapides |

### 1.3 Quand Utiliser le Stockage Colonnaire ?

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Cas d'Usage : Row vs Column                          │
│                                                                         │
│   ROW STORE (PostgreSQL standard)         COLUMN STORE (Hydra, etc.)    │
│                                                                         │
│   ✓ Transactions (OLTP)                   ✓ Analytique (OLAP)           │
│   ✓ INSERT/UPDATE/DELETE fréquents        ✓ Lecture massive             │
│   ✓ SELECT * (toutes colonnes)            ✓ Agrégations (SUM, AVG)      │
│   ✓ Requêtes par clé primaire             ✓ Scans de grandes tables     │
│   ✓ Données chaudes (accès récent)        ✓ Données historiques         │
│   ✓ < 10 millions de lignes               ✓ > 100 millions de lignes    │
│                                                                         │
│   Exemples :                              Exemples :                    │
│   • Application web classique             • Data warehouse              │
│   • E-commerce (commandes)                • Business Intelligence       │
│   • CRM, ERP                              • Logs et événements          │
│   • Sessions utilisateur                  • Time series analytics       │
│                                           • Machine Learning features   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.4 Compression dans le Stockage Colonnaire

Le stockage colonnaire permet une compression exceptionnelle car les valeurs d'une même colonne sont souvent similaires.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Techniques de Compression                            │
│                                                                         │
│   1. RUN-LENGTH ENCODING (RLE)                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │   Avant : [France, France, France, France, USA, USA, USA]       │   │
│   │   Après : [(France, 4), (USA, 3)]                               │   │
│   │   Compression : 70%+                                            │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   2. DICTIONARY ENCODING                                                │
│   ┌──────────────────────────────────────────────────────────────────┐  │
│   │   Dictionnaire : {0: "Électronique", 1: "Vêtements", 2: "Maison"}│  │
│   │   Données : [0, 0, 1, 2, 0, 1, 2, 2, 0, ...]                     │  │
│   │   Compression : 80%+                                             │  │
│   └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│   3. DELTA ENCODING (pour valeurs séquentielles)                        │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │   Avant : [1000, 1001, 1002, 1003, 1004, 1005]                  │   │
│   │   Après : [1000, +1, +1, +1, +1, +1]                            │   │
│   │   Compression : 60%+                                            │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   4. BIT-PACKING                                                        │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │   Valeurs 0-15 stockées sur 4 bits au lieu de 32                │   │
│   │   Compression : 87.5%                                           │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   Résultat typique : 1 TB de données → 100-200 GB compressé             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.5 OLTP vs OLAP : Le Dilemme Traditionnel

Historiquement, les entreprises devaient maintenir deux systèmes séparés :

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Architecture Traditionnelle                          │
│                                                                         │
│   Applications                          Analytique / BI                 │
│       │                                      │                          │
│       ▼                                      ▼                          │
│   ┌─────────────┐      ETL           ┌─────────────┐                    │
│   │    OLTP     │ ─────────────────► │    OLAP     │                    │
│   │ (PostgreSQL)│   (nuit/batch)     │ (Snowflake, │                    │
│   │             │                    │  Redshift)  │                    │
│   └─────────────┘                    └─────────────┘                    │
│                                                                         │
│   Problèmes :                                                           │
│   • Données dupliquées                                                  │
│   • Latence (données pas à jour)                                        │
│   • Coût de deux systèmes                                               │
│   • Complexité ETL                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    Architecture HTAP (Hybride)                          │
│                                                                         │
│   Applications          Analytique / BI                                 │
│       │                      │                                          │
│       └──────────┬───────────┘                                          │
│                  ▼                                                      │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    PostgreSQL + Columnar                        │   │
│   │                                                                 │   │
│   │   ┌─────────────────┐    ┌─────────────────┐                    │   │
│   │   │   Row Tables    │    │ Columnar Tables │                    │   │
│   │   │   (OLTP)        │    │   (OLAP)        │                    │   │
│   │   └─────────────────┘    └─────────────────┘                    │   │
│   │                                                                 │   │
│   │   Même base, même SQL, temps réel !                             │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   Avantages :                                                           │
│   • Une seule source de vérité                                          │
│   • Données toujours à jour                                             │
│   • Réduction des coûts                                                 │
│   • Simplicité opérationnelle                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Partie 2 : Hydra - PostgreSQL OLAP Open Source

### 2.1 Présentation de Hydra

**Hydra** est un projet open source qui ajoute le stockage colonnaire à PostgreSQL, permettant des requêtes analytiques jusqu'à 100× plus rapides.

#### Caractéristiques Principales

| Caractéristique | Description |
|-----------------|-------------|
| **Open Source** | Licence Apache 2.0 |
| **Compatible PostgreSQL** | Extension native, SQL standard |
| **Vectorisé** | Traitement SIMD pour les agrégations |
| **Compressé** | Compression automatique par colonne |
| **Parallélisé** | Utilise les workers parallèles PostgreSQL |
| **Hybride** | Mix row/columnar dans la même base |

### 2.2 Architecture Hydra

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Architecture Hydra                                   │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                      PostgreSQL Server                          │   │
│   │                                                                 │   │
│   │   ┌─────────────────────────────────────────────────────────┐   │   │
│   │   │                    Query Planner                        │   │   │
│   │   │         (choisit row ou columnar selon la requête)      │   │   │
│   │   └───────────────────────┬─────────────────────────────────┘   │   │
│   │                           │                                     │   │
│   │           ┌───────────────┴───────────────┐                     │   │
│   │           ▼                               ▼                     │   │
│   │   ┌─────────────────┐           ┌─────────────────┐             │   │
│   │   │   Heap Tables   │           │ Columnar Tables │             │   │
│   │   │   (Row Store)   │           │ (Hydra Engine)  │             │   │
│   │   │                 │           │                 │             │   │
│   │   │   • users       │           │   • events      │             │   │
│   │   │   • orders      │           │   • logs        │             │   │
│   │   │   • products    │           │   • metrics     │             │   │
│   │   │                 │           │                 │             │   │
│   │   │   Accès OLTP    │           │   Accès OLAP    │             │   │
│   │   └─────────────────┘           └─────────────────┘             │   │
│   │                                                                 │   │
│   │   ┌─────────────────────────────────────────────────────────┐   │   │
│   │   │              Vectorized Execution Engine                │   │   │
│   │   │                                                         │   │   │
│   │   │   • Traitement par batch (1024 tuples)                  │   │   │
│   │   │   • Instructions SIMD (AVX2, AVX-512)                   │   │   │
│   │   │   • Parallélisation automatique                         │   │   │
│   │   │                                                         │   │   │
│   │   └─────────────────────────────────────────────────────────┘   │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 Installation de Hydra

#### Avec Docker (Recommandé pour débuter)

```bash
# Lancer Hydra avec Docker
docker run -d \
  --name hydra \
  -e POSTGRES_PASSWORD=secret \
  -p 5432:5432 \
  ghcr.io/hydradatabase/hydra:latest

# Se connecter
psql -h localhost -U postgres
```

#### Installation depuis les Sources

```bash
# Prérequis
sudo apt install postgresql-server-dev-16 build-essential

# Cloner et compiler
git clone https://github.com/hydradatabase/hydra.git  
cd hydra/columnar  
make  
sudo make install  

# Activer l'extension
psql -c "CREATE EXTENSION columnar;"
```

### 2.4 Utilisation de Hydra

#### Créer une Table Colonnaire

```sql
-- Activer l'extension
CREATE EXTENSION IF NOT EXISTS columnar;

-- Créer une table colonnaire (syntaxe USING)
CREATE TABLE events (
    id BIGSERIAL,
    event_time TIMESTAMP NOT NULL,
    user_id INTEGER,
    event_type VARCHAR(50),
    properties JSONB,
    country VARCHAR(2),
    device VARCHAR(20),
    revenue NUMERIC(10,2)
) USING columnar;

-- Vérifier le type de stockage
SELECT relname, relam::regam  
FROM pg_class  
WHERE relname = 'events';  
```

#### Convertir une Table Existante

```sql
-- Méthode 1 : Créer une nouvelle table colonnaire
CREATE TABLE events_columnar (LIKE events) USING columnar;  
INSERT INTO events_columnar SELECT * FROM events;  
DROP TABLE events;  
ALTER TABLE events_columnar RENAME TO events;  

-- Méthode 2 : Utiliser la fonction de conversion
SELECT columnar.alter_table_set_access_method('events', 'columnar');
```

#### Options de Compression

```sql
-- Configurer la compression par table
SELECT columnar.alter_columnar_table_set(
    'events',
    compression => 'zstd',          -- Algorithme : zstd, lz4, none
    stripe_row_limit => 150000,     -- Lignes par stripe
    chunk_group_row_limit => 10000  -- Lignes par chunk
);

-- Voir les options actuelles
SELECT * FROM columnar.options WHERE relation = 'events'::regclass;
```

### 2.5 Requêtes Analytiques avec Hydra

```sql
-- Exemple : Analyse des événements (très rapide sur columnar)
-- Cette requête ne lit que les colonnes nécessaires

-- Revenus par pays et type d'appareil
SELECT
    country,
    device,
    COUNT(*) AS total_events,
    SUM(revenue) AS total_revenue,
    AVG(revenue) AS avg_revenue
FROM events  
WHERE event_time >= '2025-01-01'  
  AND event_time < '2025-02-01'
GROUP BY country, device  
ORDER BY total_revenue DESC;  

-- Tendance journalière
SELECT
    DATE_TRUNC('day', event_time) AS jour,
    event_type,
    COUNT(*) AS occurrences
FROM events  
WHERE event_time >= NOW() - INTERVAL '30 days'  
GROUP BY 1, 2  
ORDER BY 1, 3 DESC;  

-- Analyse de cohorte utilisateur
SELECT
    DATE_TRUNC('week', first_event) AS cohorte,
    COUNT(DISTINCT user_id) AS nouveaux_users,
    SUM(total_revenue) AS revenue
FROM (
    SELECT
        user_id,
        MIN(event_time) AS first_event,
        SUM(revenue) AS total_revenue
    FROM events
    GROUP BY user_id
) user_stats
GROUP BY 1  
ORDER BY 1;  
```

### 2.6 Performances Hydra vs Row Store

```sql
-- Benchmark : Table de 100 millions de lignes

-- Configuration test
CREATE TABLE events_row (LIKE events_columnar);  -- Row store
-- ... insérer 100M lignes identiques ...

-- Requête d'agrégation
EXPLAIN ANALYZE  
SELECT  
    country,
    SUM(revenue),
    COUNT(*)
FROM events_columnar  -- ou events_row  
WHERE event_time >= '2025-01-01'  
GROUP BY country;  
```

**Résultats Typiques** :

| Métrique | Row Store | Columnar (Hydra) | Amélioration |
|----------|-----------|------------------|--------------|
| **Temps d'exécution** | 45 secondes | 0.8 secondes | **56×** |
| **Données lues** | 12 GB | 180 MB | **67×** |
| **Taille stockage** | 15 GB | 2.1 GB | **7×** |

### 2.7 Bonnes Pratiques Hydra

```sql
-- 1. Choisir les bonnes tables pour le columnar
-- ✓ Tables de faits (événements, logs, métriques)
-- ✓ Tables historiques rarement modifiées
-- ✓ Tables > 1 million de lignes
-- ✗ Tables de dimensions petites (utiliser row)
-- ✗ Tables avec UPDATE/DELETE fréquents

-- 2. Optimiser les stripes pour votre workload
-- Plus de lignes par stripe = meilleure compression
-- Moins de lignes par stripe = meilleure granularité de lecture
SELECT columnar.alter_columnar_table_set(
    'events',
    stripe_row_limit => 150000  -- Défaut pour la plupart des cas
);

-- 3. Utiliser le partitionnement avec columnar
CREATE TABLE events_partitioned (
    id BIGSERIAL,
    event_time TIMESTAMP NOT NULL,
    user_id INTEGER,
    event_type VARCHAR(50),
    revenue NUMERIC(10,2)
) PARTITION BY RANGE (event_time);

-- Partitions récentes en row (pour les écritures)
CREATE TABLE events_2025_current PARTITION OF events_partitioned
    FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');

-- Partitions historiques en columnar (pour l'analytique)
CREATE TABLE events_2024_q4 PARTITION OF events_partitioned
    FOR VALUES FROM ('2024-10-01') TO ('2025-01-01')
    USING columnar;

-- 4. Batch les insertions (éviter les petits INSERTs)
-- Mauvais : INSERT INTO events VALUES (...); -- 1 ligne
-- Bon : INSERT INTO events SELECT ... FROM staging; -- milliers de lignes
-- Ou : COPY events FROM '/data/events.csv';
```

---

## Partie 3 : pg_analytics (ParadeDB)

### 3.1 Présentation de pg_analytics

**pg_analytics** est une extension développée par ParadeDB qui transforme PostgreSQL en un moteur analytique haute performance. Elle utilise le format de fichier **Parquet** et le moteur d'exécution **DataFusion** (Apache Arrow).

#### Différences avec Hydra

| Critère | Hydra | pg_analytics |
|---------|-------|--------------|
| **Format** | Format columnar natif | Parquet (standard industrie) |
| **Moteur** | Extension PostgreSQL | DataFusion (Rust) |
| **Écosystème** | PostgreSQL-centric | Compatible data lakehouse |
| **Export** | Via COPY | Fichiers Parquet natifs |
| **Maturité** | Plus mature | Plus récent, très actif |

### 3.2 Architecture pg_analytics

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Architecture pg_analytics                            │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                      PostgreSQL                                 │   │
│   │                                                                 │   │
│   │   ┌─────────────────────────────────────────────────────────┐   │   │
│   │   │                  pg_analytics Extension                 │   │   │
│   │   │                                                         │   │   │
│   │   │   ┌─────────────────┐    ┌─────────────────┐            │   │   │
│   │   │   │   SQL Parser    │───►│  Query Rewriter │            │   │   │
│   │   │   │  (PostgreSQL)   │    │   (Détecte si   │            │   │   │
│   │   │   │                 │    │   analytique)   │            │   │   │
│   │   │   └─────────────────┘    └────────┬────────┘            │   │   │
│   │   │                                   │                     │   │   │
│   │   │                    ┌──────────────┴──────────────┐      │   │   │
│   │   │                    ▼                             ▼      │   │   │
│   │   │            ┌─────────────┐              ┌─────────────┐ │   │   │
│   │   │            │ PostgreSQL  │              │  DataFusion │ │   │   │
│   │   │            │  Executor   │              │   Engine    │ │   │   │
│   │   │            │  (Row ops)  │              │ (Vectorized)│ │   │   │
│   │   │            └─────────────┘              └──────┬──────┘ │   │   │
│   │   │                                                │        │   │   │
│   │   │                                                ▼        │   │   │
│   │   │                                       ┌─────────────┐   │   │   │
│   │   │                                       │   Apache    │   │   │   │
│   │   │                                       │   Arrow     │   │   │   │
│   │   │                                       │ (In-memory) │   │   │   │
│   │   │                                       └─────────────┘   │   │   │
│   │   │                                                         │   │   │
│   │   └─────────────────────────────────────────────────────────┘   │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                    │                                    │
│                                    ▼                                    │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                    Stockage Parquet                             │   │
│   │                                                                 │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│   │   │ events.     │  │ metrics.    │  │  logs.      │             │   │
│   │   │ parquet     │  │ parquet     │  │  parquet    │             │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘             │   │
│   │                                                                 │   │
│   │   Compatible avec : Spark, DuckDB, Polars, DataBricks, etc.     │   │
│   │                                                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3 Installation de pg_analytics

#### Avec Docker (ParadeDB)

```bash
# ParadeDB inclut pg_analytics + pg_search
docker run -d \
  --name paradedb \
  -e POSTGRES_PASSWORD=secret \
  -p 5432:5432 \
  paradedb/paradedb:latest

# Se connecter
psql -h localhost -U postgres
```

#### Installation Manuelle

```bash
# Télécharger depuis les releases GitHub
wget https://github.com/paradedb/paradedb/releases/download/v0.x.x/pg_analytics-pg16.deb  
sudo dpkg -i pg_analytics-pg16.deb  

# Activer dans postgresql.conf
shared_preload_libraries = 'pg_analytics'

# Redémarrer PostgreSQL
sudo systemctl restart postgresql

# Créer l'extension
psql -c "CREATE EXTENSION pg_analytics;"
```

### 3.4 Utilisation de pg_analytics

#### Créer une Table Parquet

```sql
-- Activer l'extension
CREATE EXTENSION IF NOT EXISTS pg_analytics;

-- Créer une table avec stockage Parquet
CREATE TABLE web_analytics (
    event_id UUID DEFAULT gen_random_uuid(),
    timestamp TIMESTAMP NOT NULL,
    session_id UUID,
    user_id INTEGER,
    page_url TEXT,
    referrer TEXT,
    country CHAR(2),
    device_type VARCHAR(20),
    browser VARCHAR(50),
    load_time_ms INTEGER,
    is_bounce BOOLEAN
) USING parquet;

-- Insérer des données
INSERT INTO web_analytics (timestamp, user_id, page_url, country, device_type, load_time_ms, is_bounce)  
SELECT  
    NOW() - (random() * INTERVAL '30 days'),
    (random() * 100000)::INTEGER,
    '/page/' || (random() * 100)::INTEGER,
    (ARRAY['US', 'FR', 'DE', 'UK', 'JP'])[1 + (random() * 4)::INTEGER],
    (ARRAY['mobile', 'desktop', 'tablet'])[1 + (random() * 2)::INTEGER],
    (random() * 5000)::INTEGER,
    random() > 0.7
FROM generate_series(1, 1000000);
```

#### Requêtes Analytiques

```sql
-- Performance par pays et appareil
SELECT
    country,
    device_type,
    COUNT(*) AS page_views,
    AVG(load_time_ms)::INTEGER AS avg_load_time,
    SUM(CASE WHEN is_bounce THEN 1 ELSE 0 END) * 100.0 / COUNT(*) AS bounce_rate
FROM web_analytics  
WHERE timestamp >= NOW() - INTERVAL '7 days'  
GROUP BY country, device_type  
ORDER BY page_views DESC;  

-- Pages les plus lentes
SELECT
    page_url,
    COUNT(*) AS views,
    PERCENTILE_CONT(0.50) WITHIN GROUP (ORDER BY load_time_ms) AS p50_ms,
    PERCENTILE_CONT(0.95) WITHIN GROUP (ORDER BY load_time_ms) AS p95_ms,
    MAX(load_time_ms) AS max_ms
FROM web_analytics  
GROUP BY page_url  
HAVING COUNT(*) > 100  
ORDER BY p95_ms DESC  
LIMIT 20;  
```

### 3.5 Intégration avec le Data Lakehouse

Un avantage majeur de pg_analytics est son intégration avec l'écosystème data lakehouse.

#### Lire des Fichiers Parquet Externes

```sql
-- Créer une table externe pointant vers S3
CREATE FOREIGN TABLE external_events ()  
SERVER parquet_server  
OPTIONS (  
    files 's3://my-bucket/events/*.parquet'
);

-- Interroger directement les fichiers Parquet
SELECT
    event_type,
    COUNT(*),
    SUM(revenue)
FROM external_events  
WHERE event_date = '2025-01-15'  
GROUP BY event_type;  
```

#### Exporter vers Parquet

```sql
-- Exporter une requête vers un fichier Parquet
COPY (
    SELECT * FROM events
    WHERE event_time >= '2025-01-01'
) TO '/data/exports/events_jan2025.parquet'
WITH (FORMAT parquet);
```

### 3.6 pg_search : Recherche Full-Text avec pg_analytics

ParadeDB inclut aussi **pg_search**, une extension de recherche full-text ultra-rapide basée sur Tantivy (équivalent Rust de Lucene).

```sql
-- Activer pg_search
CREATE EXTENSION pg_search;

-- Créer un index BM25 pour la recherche
CREATE INDEX idx_articles_search ON articles  
USING bm25 (title, content)  
WITH (key_field = 'id');  

-- Recherche full-text performante
SELECT
    id,
    title,
    paradedb.score(id) AS relevance
FROM articles  
WHERE articles @@@ 'PostgreSQL AND performance'  
ORDER BY relevance DESC  
LIMIT 10;  

-- Recherche hybride : full-text + filtres SQL
SELECT *  
FROM articles  
WHERE articles @@@ 'machine learning'  
  AND published_at >= '2025-01-01'
  AND category = 'technology'
ORDER BY paradedb.score(id) DESC;
```

---

## Partie 4 : Autres Solutions Colonnaires

### 4.1 Citus Columnar

Citus inclut également un stockage colonnaire, utile pour les workloads analytiques distribués.

```sql
-- Avec Citus
CREATE EXTENSION citus_columnar;

CREATE TABLE analytics_events (...) USING columnar;

-- Distribuer la table colonnaire
SELECT create_distributed_table('analytics_events', 'tenant_id');
```

### 4.2 TimescaleDB Compression

TimescaleDB offre une compression colonnaire pour les séries temporelles.

```sql
-- Activer la compression sur une hypertable
ALTER TABLE metrics SET (
    timescaledb.compress,
    timescaledb.compress_segmentby = 'device_id',
    timescaledb.compress_orderby = 'time DESC'
);

-- Compresser les anciennes données
SELECT compress_chunk(c)  
FROM show_chunks('metrics', older_than => INTERVAL '7 days') c;  

-- Les chunks compressés utilisent le stockage colonnaire
```

### 4.3 ClickHouse avec clickhouse_fdw

Pour des besoins analytiques extrêmes, vous pouvez fédérer PostgreSQL avec ClickHouse.

```sql
-- Installer le FDW
CREATE EXTENSION clickhouse_fdw;

-- Configurer le serveur ClickHouse
CREATE SERVER clickhouse_server  
FOREIGN DATA WRAPPER clickhouse_fdw  
OPTIONS (host 'clickhouse.example.com', port '8123');  

-- Mapper une table ClickHouse
CREATE FOREIGN TABLE ch_events (
    event_time DateTime,
    user_id UInt32,
    event_type String,
    revenue Float64
) SERVER clickhouse_server
OPTIONS (table_name 'events');

-- Joindre données PostgreSQL et ClickHouse !
SELECT
    u.name,
    COUNT(*) AS events,
    SUM(e.revenue) AS total_revenue
FROM users u  
JOIN ch_events e ON u.id = e.user_id  
GROUP BY u.name;  
```

### 4.4 DuckDB avec pg_duckdb

DuckDB est un moteur analytique embarqué. pg_duckdb l'intègre dans PostgreSQL.

```sql
-- Installer pg_duckdb
CREATE EXTENSION pg_duckdb;

-- Exécuter des requêtes analytiques via DuckDB
SELECT duckdb.query($$
    SELECT
        country,
        SUM(revenue) AS total
    FROM read_parquet('s3://bucket/events/*.parquet')
    GROUP BY country
    ORDER BY total DESC
$$);
```

---

## Partie 5 : Comparaison des Solutions

### 5.1 Tableau Comparatif

| Critère | Hydra | pg_analytics | Citus Columnar | TimescaleDB |
|---------|-------|--------------|----------------|-------------|
| **Format** | Natif | Parquet | Natif | Natif |
| **Compression** | ★★★★ | ★★★★★ | ★★★★ | ★★★★ |
| **Vitesse analytique** | ★★★★ | ★★★★★ | ★★★★ | ★★★ |
| **Intégration PostgreSQL** | ★★★★★ | ★★★★ | ★★★★★ | ★★★★★ |
| **Écosystème externe** | ★★ | ★★★★★ | ★★ | ★★★ |
| **Distribution** | Non | Non | Oui | Limitée |
| **Maturité** | ★★★★ | ★★★ | ★★★★ | ★★★★★ |
| **Cas d'usage** | OLAP général | Data lakehouse | Multi-tenant | Time series |

### 5.2 Arbre de Décision

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Choisir une Solution Colonnaire                      │
│                                                                         │
│   Besoin principal ?                                                    │
│   │                                                                     │
│   ├─► Séries temporelles ──────────────────────► TimescaleDB            │
│   │                                                                     │
│   ├─► Multi-tenant à grande échelle ──────────► Citus Columnar          │
│   │                                                                     │
│   ├─► Intégration data lakehouse (Parquet) ───► pg_analytics            │
│   │                                                                     │
│   └─► Analytique PostgreSQL simple ───────────► Hydra                   │
│                                                                         │
│   Considérations secondaires :                                          │
│   │                                                                     │
│   ├─► Besoin d'exporter vers Spark/Databricks ?                         │
│   │   └─► pg_analytics (format Parquet natif)                           │
│   │                                                                     │
│   ├─► Stabilité et maturité prioritaires ?                              │
│   │   └─► TimescaleDB ou Citus                                          │
│   │                                                                     │
│   └─► Performance pure sur agrégations ?                                │
│       └─► pg_analytics ou ClickHouse (via FDW)                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Partie 6 : Bonnes Pratiques et Patterns

### 6.1 Pattern : Hot/Warm/Cold Data

Utilisez différents types de stockage selon l'âge des données.

```sql
-- Table partitionnée par temps
CREATE TABLE events (
    id BIGSERIAL,
    event_time TIMESTAMP NOT NULL,
    event_type VARCHAR(50),
    data JSONB
) PARTITION BY RANGE (event_time);

-- HOT : Données récentes en row store (écritures fréquentes)
CREATE TABLE events_current PARTITION OF events
    FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');
    -- Utilise le heap standard (row)

-- WARM : Données du mois précédent en columnar (lectures analytiques)
CREATE TABLE events_2024_12 PARTITION OF events
    FOR VALUES FROM ('2024-12-01') TO ('2025-01-01')
    USING columnar;

-- COLD : Données anciennes archivées (stockage externe)
-- → Exporter vers S3 en Parquet et supprimer de PostgreSQL

-- Procédure de rotation mensuelle
CREATE OR REPLACE PROCEDURE rotate_partitions()  
LANGUAGE plpgsql AS $$  
DECLARE  
    old_partition TEXT;
BEGIN
    -- Convertir la partition du mois précédent en columnar
    old_partition := 'events_' || TO_CHAR(NOW() - INTERVAL '1 month', 'YYYY_MM');
    EXECUTE format('ALTER TABLE %I SET ACCESS METHOD columnar', old_partition);

    -- Créer la nouvelle partition pour le mois suivant
    EXECUTE format(
        'CREATE TABLE events_%s PARTITION OF events FOR VALUES FROM (%L) TO (%L)',
        TO_CHAR(NOW() + INTERVAL '1 month', 'YYYY_MM'),
        DATE_TRUNC('month', NOW() + INTERVAL '1 month'),
        DATE_TRUNC('month', NOW() + INTERVAL '2 months')
    );
END;
$$;
```

### 6.2 Pattern : Tables de Faits et Dimensions

```sql
-- Dimensions : Petites tables, accès par clé → Row store
CREATE TABLE dim_products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200),
    category VARCHAR(100),
    price NUMERIC(10,2)
);  -- Row store par défaut

CREATE TABLE dim_customers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200),
    email VARCHAR(255),
    country VARCHAR(2)
);  -- Row store par défaut

-- Faits : Grande table, agrégations → Columnar
CREATE TABLE fact_sales (
    sale_id BIGSERIAL,
    sale_time TIMESTAMP NOT NULL,
    product_id INTEGER REFERENCES dim_products(id),
    customer_id INTEGER REFERENCES dim_customers(id),
    quantity INTEGER,
    amount NUMERIC(12,2),
    discount NUMERIC(5,2)
) USING columnar;

-- Les jointures fonctionnent normalement
SELECT
    p.category,
    c.country,
    SUM(f.amount) AS total_sales,
    COUNT(*) AS num_transactions
FROM fact_sales f  
JOIN dim_products p ON f.product_id = p.id  
JOIN dim_customers c ON f.customer_id = c.id  
WHERE f.sale_time >= '2025-01-01'  
GROUP BY p.category, c.country  
ORDER BY total_sales DESC;  
```

### 6.3 Pattern : Vues Matérialisées Colonnaires

```sql
-- Pré-agréger les données pour des dashboards plus rapides
CREATE MATERIALIZED VIEW daily_metrics  
USING columnar AS  
SELECT  
    DATE_TRUNC('day', event_time) AS day,
    event_type,
    country,
    COUNT(*) AS event_count,
    COUNT(DISTINCT user_id) AS unique_users,
    SUM(revenue) AS total_revenue
FROM events  
GROUP BY 1, 2, 3;  

-- Rafraîchir périodiquement
REFRESH MATERIALIZED VIEW CONCURRENTLY daily_metrics;

-- Index pour les requêtes courantes
CREATE INDEX idx_daily_metrics_day ON daily_metrics(day);

-- Requêtes sur la vue (ultra-rapide)
SELECT * FROM daily_metrics  
WHERE day >= NOW() - INTERVAL '7 days'  
ORDER BY day DESC, total_revenue DESC;  
```

### 6.4 Monitoring des Tables Colonnaires

```sql
-- Statistiques sur les tables colonnaires (Hydra)
SELECT
    c.relname AS table_name,
    pg_size_pretty(pg_relation_size(c.oid)) AS size,
    s.stripe_num AS num_stripes,
    s.row_count AS total_rows,
    s.chunk_count AS num_chunks
FROM pg_class c  
JOIN columnar.stripe s ON s.storage_id = columnar.get_storage_id(c.oid)  
WHERE c.relam = (SELECT oid FROM pg_am WHERE amname = 'columnar');  

-- Ratio de compression
SELECT
    relname,
    pg_size_pretty(pg_relation_size(oid)) AS compressed_size,
    pg_size_pretty(
        (SELECT SUM(row_count) * AVG(chunk_group_row_limit) * 100
         FROM columnar.stripe
         WHERE storage_id = columnar.get_storage_id(oid))
    ) AS estimated_uncompressed
FROM pg_class  
WHERE relam = (SELECT oid FROM pg_am WHERE amname = 'columnar');  
```

---

## Résumé

Ce chapitre a exploré le stockage colonnaire dans PostgreSQL :

| Concept | Ce qu'il faut retenir |
|---------|----------------------|
| **Row vs Column** | Row pour OLTP, Column pour OLAP |
| **Compression** | 5-10× réduction de taille |
| **Hydra** | Extension columnar native PostgreSQL |
| **pg_analytics** | Format Parquet, écosystème data lakehouse |
| **HTAP** | Transactions + Analytics dans une seule base |

### Points Clés

- Le stockage colonnaire excelle pour les agrégations sur grandes tables
- Hydra est le choix simple pour rester 100% PostgreSQL
- pg_analytics ouvre PostgreSQL à l'écosystème data lakehouse
- Combinez row et columnar selon le pattern d'accès aux données
- Le partitionnement temporel facilite la gestion hot/warm/cold

### L'Avenir

Le stockage colonnaire dans PostgreSQL évolue rapidement :
- Amélioration continue des performances vectorisées
- Meilleure intégration avec les formats lakehouse (Iceberg, Delta Lake)
- Potentielle intégration native dans PostgreSQL core
- Convergence OLTP/OLAP vers le "HTAP" unifié

---


⏭️ [Les apports majeurs de PostgreSQL 18](/21-conclusion-et-perspectives/03-apports-majeurs-pg18.md)
